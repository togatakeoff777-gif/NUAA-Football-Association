# F-009 — Nginx media request size contract

Status: **FIXED (仓库候选静态验证通过；runtime test deferred by scope)**

## 修改文件

- `ops/nginx/nuaafa-production.disabled.conf.example`
- `ops/nginx/README.md`
- `scripts/test-security-nginx.mjs`
- `package.json`

## 实现方式

- 依据当前代码事实（PDF 最大 20 MiB，完整 request 最大 21 MiB），仓库 disabled candidate 设置 `client_max_body_size 25m`。
- 模板顶端明确标注 disabled candidate only，需人工 review、TLS provisioning、`nginx -t` 后才可能启用。

## 新增测试

- 静态断言存在且仅采用 `client_max_body_size 25m`。
- 静态断言不存在已拒绝的 `10m` limit。

## 测试结果

- `npm.cmd run test:security-nginx`: PASS

## Blocker

无代码 blocker。仓库静态门禁与 20 MiB application upload regression 已通过；真实 proxy runtime boundary test 按授权明确留待备案后的 Production Closure，本轮未启用 Nginx。

## Schema / production impact

无 schema/migration；未修改、复制、启用或 reload 生产 Nginx。
