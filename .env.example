# SQLite file used by Prisma. Keep the real database outside Git.
DATABASE_URL="file:./prisma/dev.db"

# Set strong local or production-only values. Never commit actual credentials.
REFEREE_ADMIN_PASSWORD="replace-with-a-strong-password"
REFEREE_SESSION_SECRET="replace-with-at-least-32-random-characters"
